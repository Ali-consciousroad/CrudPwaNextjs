// Updated TypeScript example for PWA setup
// File: next.config.ts
import withPWA from 'next-pwa'; // Import the PWA integration package
import { NextConfig } from 'next'; // Import the Next.js config type for better TypeScript support

// Define the base configuration for the Next.js app
const config: NextConfig = {
  reactStrictMode: true, // Enable React's strict mode for catching potential issues
};
